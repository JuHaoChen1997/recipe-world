{
  "name": "back-end",
  "version": "1.0.0",
  "description": "recipe share api server",
  "main": "server.js",
  "scripts": {
    "db:init": "psql -U postgres -f db/schema.sql",
    "db:seed": "psql -U postgres -f db/seed.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JuHao Chen",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "pg-promise": "^10.11.1"
  }
}
